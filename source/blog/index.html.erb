---
pageable: true
per_page: 10
---

<%= partial "layouts/partials/nav" %>
<div id="blog" role="main" class="blog-container">
  <% if paginate && num_pages > 1 %>
    <p>Page <%= page_number %> of <%= num_pages %></p>

    <% if prev_page %>
      <p><%= link_to 'Previous page', prev_page %></p>
    <% end %>
  <% end %>

  <ul>
    <% page_articles.each_with_index do |article, i| %>
      <li class="post">
        <h3 class="post-title"><%= link_to article.title, article %></h3>
        <h5 class="post-date"><%= article.date.strftime('%b %e, %Y') %></h5>
        <p><%= article.summary(650)  %></p>
        <%= link_to 'Read more…', article %>
      </li>
    <div class="post-end">
      <%= image_tag "coffee-blog.png", class: "post-end-image" %>
    </div>
    <% end %>
  </ul>

  <% if paginate %>
    <% if next_page %>
      <p><%= link_to 'Next page', next_page %></p>
    <% end %>
  <% end %>
</div>
